import {StyleSheet} from '@react-pdf/renderer';

const styles = StyleSheet.create({
    wrapper: {
        marginTop: 210,
    },
    consent: {
        fontSize: 9,
    },
});

export default styles;
