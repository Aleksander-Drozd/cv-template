import {StyleSheet} from '@react-pdf/renderer';

const styles = StyleSheet.create({
    wrapper: {
        marginTop: 100,
    },
    consent: {
        fontSize: 9,
    },
    credits: {
        fontSize: 8,
        marginTop: 10,
        textAlign: 'center',
    },
});

export default styles;
