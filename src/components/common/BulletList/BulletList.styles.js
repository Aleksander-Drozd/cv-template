import {StyleSheet} from '@react-pdf/renderer';

const styles = StyleSheet.create({
    itemWrapper: {
        flexDirection: 'row',
    },
    bulletPoint: {
        width: '5%',
    },
    text: {
        width: '95%',
        fontSize: 11,
        letterSpacing: 0.2,
    },
});

export default styles;
